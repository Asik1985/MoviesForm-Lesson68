@model Movie
@{
    ViewData["title"] = Model.Title;
}

<h1>@Model.Title</h1>
<form asp-action="DeleteConfirm" asp-controller="Movies" method="post">
    <input asp-for="Id" type="hidden" />
    <button type="submit" class="btn btn-primary">удалить?</button>
</form>